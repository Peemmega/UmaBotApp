/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: "com.peemmega.umadnd",
  appName: "UmaDnD",
  webDir: "uma-dashboard-ui/dist",
  server: {
    androidScheme: "https"
  }
};

module.exports = config;
